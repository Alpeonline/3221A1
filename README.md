How to use: 
In a linux environment, use the command make {fcfs,sjf,priority,rr,priority_rr} choose any of these
then run grader_student.sh to execute all the scheduling algorithms, the outputs will be visible in the folder
